# Jiang Wenqian Portfolio

静态 HTML / CSS / JavaScript 作品集，无需构建步骤。

## 运行

1. 直接打开 `index.html`。
2. 或在当前目录运行本地静态服务器，例如：`python -m http.server 4173`，然后访问 `http://localhost:4173/`。

所有图片、视频和简历均读取自 `public/`，不依赖网络素材。
